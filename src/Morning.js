import React from 'react';
import moment from "moment-timezone";
import { Card } from "react-bootstrap";

const Morning = ({item}) => 


	<Card.Body>
    <Card.Title>
    {item.title}
    </Card.Title>
    {this.props.time}{item.beginning}{item.closing}
    </Card.Body> 






export default Morning;