import React from 'react';

const MessageSelectSingle = ({item}) => (                   
    
    <option value={item.title}>
    {item.title}
    </option>
                
                
);

export default MessageSelectSingle;